import { localClient } from './localClient';

// ✅ MIMITECH Client - Komplett lokale Version
// Daten werden in LocalStorage gespeichert
// Keine externe Abhängigkeiten
export const mimitech = localClient;
export default mimitech;
